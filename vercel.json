{
  "version": 2,
  "framework": null,
  "buildCommand": "cd artifacts/khmer-mini-app && npm install && ./node_modules/.bin/vite build --config vite.config.ts",
  "outputDirectory": "artifacts/khmer-mini-app/dist/public",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ],
  "env": {
    "BASE_PATH": "/"
  }
}
